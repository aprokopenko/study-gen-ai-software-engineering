services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
      - ./package.json:/app/package.json
      - node_modules:/app/node_modules
    env_file:
      - .env
    restart: unless-stopped

volumes:
  node_modules:
